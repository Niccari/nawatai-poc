rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // 読み取りは誰でも可能（公開画像として配信）
      allow read: if true;
      // 書き込みは拒否（Admin SDK経由のAPIからのみアップロード可能）
      allow write: if false;
    }
  }
}
